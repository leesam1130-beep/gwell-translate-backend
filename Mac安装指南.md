# GWELL 翻译插件 · Mac 安装指南

> 本文是给 **第一次在苹果电脑上安装 GWELL 翻译插件** 的销售同事看的。  
> 全程 **不需要任何技术知识**，照着图一步一步点就行，预计 5 分钟搞定。

---

## 你需要先准备 2 样东西

| # | 你需要拿到 | 从哪里拿 |
|---|---|---|
| 1 | 一个名字像 `gwell-wa-crm-extension.zip` 的压缩包 | 找 **公司技术同事** 拿，一般通过微信 / 钉钉 / 邮件发给你 |
| 2 | 一个 **你的专属用户名**（例如 `alice`、`bob`）| 找 **管理员** 开通，没有用户名插件用不了 |

> **注意**：用户名是英文小写 + 数字，不要带空格、中文、@ 这些。如果你不知道自己的用户名是什么，**先问管理员**，再开始装。

---

## 第 1 步：把压缩包解压出来，放到一个固定位置

### 1.1 双击 ZIP 文件解压

Mac 上双击 `.zip` 会自动解压，你会在同一个文件夹里看到一个 **不带 .zip 后缀** 的新文件夹，比如 `gwell-wa-crm-extension`。

> `[截图 1]` Finder 里 ZIP 文件 与 解压后文件夹 并排显示的样子

### 1.2 把这个文件夹拖到一个 **以后不会动它** 的位置

**非常重要**：插件装好之后，这个文件夹 **不能删、不能改名、不能移动位置**，否则 Chrome 会找不到插件，下次开浏览器它就消失了。

推荐放到这两个位置之一：

- **方案 A（推荐）**：`文稿（Documents）` 里专门建一个文件夹叫 `GWELL插件`，把它放进去
- **方案 B**：直接放在 `应用程序（Applications）` 文件夹里

> `[截图 2]` Finder 左边栏的「文稿」位置 + 新建文件夹 `GWELL插件` + 把解压出来的文件夹拖进去

### 1.3 进去文件夹检查一下

打开你刚放好的那个文件夹，**里面应该能直接看到一个叫 `manifest.json` 的文件**（可能还有 `background.js`、`content.js`、`icons` 文件夹等等）。

> **如果你看到的是又一层文件夹（套娃）**，比如 `gwell-wa-crm-extension/gwell-wa-crm-extension/manifest.json` 这种，那你后面要选的是 **里面那一层**，不是外面那层。

> `[截图 3]` Finder 进入文件夹后看到 `manifest.json` 的画面

---

## 第 2 步：打开 Chrome 的扩展程序管理页

### 2.1 打开 Chrome 浏览器（不是 Safari！）

如果你电脑上没装 Chrome，先去 [https://www.google.com/chrome/](https://www.google.com/chrome/) 下载安装。

### 2.2 在地址栏粘贴下面这段，按回车

```
chrome://extensions/
```

> `[截图 4]` Chrome 地址栏输入 `chrome://extensions/` 的样子

或者用菜单进：

> 点 Chrome 右上角 **三个点** → **扩展程序** → **管理扩展程序**

> `[截图 5]` Chrome 右上角三个点菜单展开 → 鼠标停在「扩展程序」上 → 子菜单「管理扩展程序」

---

## 第 3 步：打开「开发者模式」开关

页面打开后，**右上角** 有一个 **「开发者模式 / Developer mode」** 的开关。**把它打开**（变成蓝色 / 开启状态）。

> `[截图 6]` 扩展程序页面右上角「开发者模式」开关在「开」状态的样子

打开之后，页面左上角会 **多出三个按钮**：
- 加载已解压的扩展程序（Load unpacked）
- 打包扩展程序（Pack extension）
- 更新（Update）

---

## 第 4 步：加载 GWELL 插件

### 4.1 点 **「加载已解压的扩展程序 / Load unpacked」** 按钮

> `[截图 7]` 左上角三个按钮中的「加载已解压的扩展程序」高亮

### 4.2 在弹出的窗口里，选你第 1 步放好的那个文件夹

**重点**：选的是 **文件夹本身**，不是文件夹里的某个文件。
- 选对：选中那个名叫 `gwell-wa-crm-extension` 的文件夹本体 → 点「选择」
- 选错：进到文件夹里面再点 `manifest.json` → ✗ 不行

> 如果选完没反应、或者报错说「找不到 manifest」，回到 **第 1.3 步** 检查一下你选的位置对不对。

> `[截图 8]` Mac 的文件选择窗口，选中 `gwell-wa-crm-extension` 文件夹时按钮「选择 / Open」可点击

### 4.3 看到 GWELL 插件出现在列表里就成功了

页面上会出现一张卡片，写着插件名字（比如 `GWELL WA CRM Extension`）、版本号、还有一个 **开/关** 的开关，确认开关是 **开** 的状态。

> `[截图 9]` 扩展程序列表里 GWELL 卡片的样子，开关在「开启」状态

> **可能会看到一条黄色警告**：「此扩展未列在 Chrome Web Store ...」  
> **这是正常的**，不用管它，也不要点「移除」。

---

## 第 5 步：把插件图标钉到工具栏上（方便点）

### 5.1 点击 Chrome 右上角的 **拼图图标**（扩展程序按钮）

> `[截图 10]` Chrome 右上角，地址栏右边的拼图图标位置

### 5.2 在弹出列表里找到 GWELL，点旁边的 **图钉** 图标

图钉变成蓝色，就说明 GWELL 图标已经钉到工具栏上了。以后直接点工具栏上的 GWELL 图标就能用。

> `[截图 11]` 拼图菜单展开，GWELL 那一行右边的图钉图标，钉上之后变蓝

---

## 第 6 步：填写你的用户名（**必做**，不填没法翻译）

### 6.1 点工具栏上的 GWELL 图标，打开插件面板

### 6.2 找到 **「设置 / Settings」** 入口

通常在面板里有一个齿轮图标，或者直接是「设置」按钮。

### 6.3 在 **用户名 / Username** 那一栏，填管理员给你的用户名

> 例如 `alice` —— 全英文小写、不要带空格、不要带 `@`。

### 6.4 点 **「保存 / Save」**，然后点 **「测试连接 / Test」**

- 看到 **「已授权 / OK / 连接成功」** → 装完了，可以用了
- 看到 **「用户名未授权 / NOT_ALLOWED」** → 你填的用户名不在白名单里，**联系管理员**确认拼写或开通
- 看到 **「需要填用户名 / USERNAME_REQUIRED」** → 你还没填，回去填完再保存

> `[截图 12]` 插件面板里「用户名」输入框 + 「测试连接」按钮 + 成功提示「已授权」

---

## 第 7 步：打开 WhatsApp Web，开始用

### 7.1 在 Chrome 里打开 [https://web.whatsapp.com/](https://web.whatsapp.com/)，扫码登录

### 7.2 进任意一个客户的聊天窗口，应该能看到 GWELL 插件的翻译按钮 / 面板

> 如果什么都没看到，**刷新一下页面**（按 `Cmd + R`）。

---

## 常见问题快速排查

| 现象 | 怎么办 |
|---|---|
| Chrome 重启之后插件不见了 | 大概率是你把第 1 步那个文件夹删了 / 移走了 / 改名了。重新放回原位 → 回到 chrome://extensions/ 点「重新加载」 |
| 看到「此扩展未列在 Chrome Web Store...」黄条 | 正常的，不影响使用，**不要点「移除」** |
| 翻译按钮点了没反应 / 一直转圈 | 1）检查 WiFi 通不通  2）打开插件设置 → 点「测试连接」看是否正常  3）实在不行重启 Chrome |
| 测试连接说「用户名未授权」 | 找管理员开通，或者检查用户名是不是拼错了（要全英文小写）|
| 测试连接说「服务器异常 / 500」 | 是后端的事，找技术同事，不是你的问题 |
| 用了几天突然翻译变慢 | 这是正常的，可能是高峰期，等几秒。如果一直不行，找技术 |
| 想换电脑 | 在新电脑上 **从头照本指南再装一遍** 就行，用户名还是同一个 |

---

## 需要帮忙找谁

- **拿不到插件压缩包 / 不知道用户名** → 找你们的 **管理员 / 项目负责人**
- **照着装失败、Chrome 报错看不懂** → 截图发给 **技术同事**，把第几步出问题、错误提示截全
- **WhatsApp Web 本身打不开、扫不了码** → 这跟 GWELL 插件无关，先确认 WhatsApp 手机 App 在线

---

> **小提示**：建议把这份文档存到手机里 / 收藏夹里，下次换电脑、或者帮新同事装的时候直接照着翻就行，不用每次都问技术。
